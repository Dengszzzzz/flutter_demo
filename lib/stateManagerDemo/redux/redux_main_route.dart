import 'package:flutter/widgets.dart';

class ReduxMainRoute extends StatelessWidget {
  const ReduxMainRoute({Key? key}) : super(key: key);

  //const ReduxMainRoute({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
